This is the same code behind the bluetooth implementation, but requires a physical connection between the arduino and the IMU.
Key differences are that this code runs at 100 hertz and generally works better due to better IMU readings. 
